# 验证记录

## 2026-09-19：原文详情阅读器（最新增量）

本轮修改 `app.js` 和 `architecture.css` 的详情组件，增加可选结构化全文阅读、逐图署名/转载说明、目录焦点和显式缺图/摘录模式。当前12条生产示例数据未改，**未导入第三方全文或原图**。全文浏览器样例为隔离的原创QA文档，不是新闻转载。

| 检查 | 本轮结果 |
|---|---|
| `tests/design-system.test.cjs` | 21项通过；Token/无衬线/品牌/控件规范 |
| `tests/prototype.test.cjs` | 87项通过；含原有业务、12条双语详情、全文块/转义/拒绝不完整文档/逐图许可与路径/目录焦点/收藏已读独立 |
| `tests/browser-regression.cjs` | Chrome 57个布局场景通过 + 5个样板宽度；无 pageerror |
| 响应式 | 中英 × 320/375/768/1024/1440；首条真实摘录详情与原创全文样例，无页面横向溢出 |
| 全文增量 | 原图比例不裁切、表格/代码局部滚动、文内目录键盘焦点、图片请求失败保留图注及提示 |
| 放大 | 全文样例中英文375px的根字号200%检查通过；不是浏览器真实缩放验收 |

证据保存在 `design/reviews/article-reader/`；`en-375-full-reader-QA.png` 必须按测试夹具标识使用，不能宣传为已接入新闻全文。完整临时浏览器截图目录为 `/tmp/abespresso-detail-work/browser`。

**未完成/遗留：** 实际全文与逐图转载材料、保真提取与完整性人工核对、生产审批与资源版本发布、复杂公式/嵌套结构支持。Noto Sans SC仍未打包，中文依赖本机回退。未验Safari/Firefox、屏幕阅读器、真实200%/400%缩放和实际外链全文阅读。以下旧记录不代表本轮新增能力已全面上线。

## 真实原文数据与直达来源 · 2026-09-19（当前版本）

### 范围

- 用 12 条真实原始文章替换概念性新闻；日期范围 2024-10-22—2025-03-12。原型期号/路由仍为 2026-09-14，并明确不是新闻日期。
- 首页、筛选、收藏、搜索、12 条详情一致使用原文标题、短摘录、发布方、真实日期。删除旧生成式 deck/what/why/how/impact；不增加翻译或自动摘要。
- 来源新标签直达具体文章；标题继续进入站内详情。HTTP(S) URL 校验、来源文本转义、缺失摘录不补写、缺失日期不套用期号。
- 保留本地收藏/已读、筛选、归档、完成确认、原存储键；保留视频但标明延伸学习，不冒充本条新闻视频。

### 通过

- `node --check app.js`、`node --check content.js`：通过。
- `tests/design-system.test.cjs`：**21 项**。
- `tests/prototype.test.cjs`：**62 项**。包含双语界面下原文不改写、来源/日期精确一致、12 条详情、筛选/收藏/搜索、转义与危险链接、缺失摘录、核对清单完整性，以及既有业务流程。
- `tests/browser-regression.cjs`：**43 个页面/附加场景 + 5 个样板宽度**，无页面脚本错误。中英文 × 320/375/768/1024/1440px × Today/首条详情/Archive/Saved；包含小字号、触控区、溢出、标题层级、焦点、状态持久化等检查。
- 新增 **12 次来源链接实际点击**，断言新标签目标 URL、`window.opener === null`、外链点击不改变本地阅读/收藏状态。该浏览器步骤拦截第三方响应以稳定测试导航行为，不冒充远端页面视觉验收。
- `python3 tests/verify-news-sources.py`：**12/12**；独立真实联网请求，HTTP 200 且标题与短摘录均在原页面中。日期依据见 `news-sources.json` 的 `dateEvidence`，并非从 HTTP 更新时间推断。
- 人工查看中文桌面首页、英文 320px 手机首页、中文 375px 详情截图：标题、引用、来源、日期与独立操作可读，未见重叠。自动断言与截图审阅不等于历史基线逐像素比较。

### 证据及未验证项

- 本机临时证据：`/tmp/abespresso-real-news-20260919/`，含 `results.json`、`prototype-results.json`、`design-results.json`、`source-results.json` 与截图。
- 持久来源清单：`design/news-sources.json`；数据契约：`design/CARD-CONTENT.md`。
- 原文链接是本轮检查时可用，后续第三方改版、链接失效或地域限制不在持续监控范围内；发布方的性能/产品/研究主张未作独立事实核验。
- 当前是历史静态原文选编，**未接入实时采集、模型、账号或云端发布，未部署线上**。
- **未验证**：Safari/Firefox、屏幕阅读器、真实浏览器级缩放、YouTube 实际播放与地域可用性。200% 测试是 CSS 根字号压力检查，不是浏览器缩放认证。
- **遗留**：Noto Sans SC 分片未打包，中文依赖本机回退；ZIP 未重新生成。既有收藏/已读 ID 保留，现对应替换后的同编号内容。

---

## 历史记录：仅移除卡片价值解释 · 2026-09-19

本轮范围：移除首页、筛选结果和复用卡片的收藏列表中的“为什么重要”，删除列表专用 `scanWhy` 数据及废弃样式，同步规范与样板。详情页解释章节保留；原型没有模型生成服务，本次没有修改线上生成链路。12条（Top 5 + 7）、原摘要、资料、视频、独立收藏/已读语义、日期、路由和存储键保留。

### 本轮通过

- `node --check app.js`、`node --check content.js`：通过。
- `tests/design-system.test.cjs`：**21项通过**，新增已删除字段、组件和样板不回流的契约。
- `tests/prototype.test.cjs`：**57项通过**，新增双语卡片无价值解释且保留12条摘要/资料/操作的检查，筛选与收藏断言同步；保留详情测试。
- `tests/browser-regression.cjs`：**43个页面/附加场景检查、5个规范样板宽度通过**，`errors` 为空；保留键盘、弹窗焦点、收藏/已读独立、筛选、完成状态持久化和视频同意检查。
- 布局矩阵覆盖中英文 × 320/375/768/1024/1440px × Today/首条详情/Archive/Saved；首页新增无价值解释、12条摘要保留检查。
- 人工重点查看中英文1440px桌面及375px手机首页截图；移除解释后标题、摘要、来源及操作仍可见，未发现重叠或截断。此次是自动布局断言与截图复核，**不是历史基线逐像素比较**。

### 执行证据与边界

- 截图和浏览器结果：`/tmp/abespresso-card-structure-20260919/`；静态/业务结果：`/tmp/abespresso-design-results.json`、`/tmp/abespresso-prototype-results.json`（临时本机文件）。
- Node和Playwright使用Codex随附运行时；jsdom使用 `/private/tmp/abespresso-design-qa/deps/node_modules`；浏览器为本机Chrome独立无头上下文。
- 没有打包Noto Sans SC，中文仍依赖本机无衬线回退；Safari、Firefox、跨系统、屏幕阅读器、真实浏览器缩放与400%回流未验证。200%测试仅放大根字体。
- 下方v1.2记录是历史验收，不代表本轮额外重新执行了字体、对比度或所有详情的人工验收。

---

# v1.2 验证记录（历史 · 2026-09-17）

验证日期：2026-09-17（America/Los_Angeles）。范围是当前源文件，不是历史 ZIP。

## 自动检查：通过

- `node tests/design-system.test.cjs`：**20 项通过**。保留原17项，新增业务字号 Token/旧断点/important 禁用、共享组件加载顺序、阅读尺寸与焦点值契约。
- `node tests/prototype.test.cjs`：**56 项通过**。保留原55项，新增标题先于图片、资料标签、日期与原生进度语义；原检查含24份双语详情、Top5+7、筛选/搜索/归档、收藏和已读独立、视频加载确认、损坏/禁用存储降级。
- `node --check app.js`、`node --check design/styleguide.js`：通过。
- 原品牌 SVG 摘要检查继续通过；没有修改 `content.js`、既有路由或 `afi-editorial-prototype-v1` 存储键。

## 真实浏览器：通过

`tests/browser-regression.cjs` 使用 Playwright 驱动本机 Chrome 的独立无头上下文，不修改用户日常浏览器记录。

- **40 个页面组合**：中文/英文 × 320/375/768/1024/1440px × Today/首条详情/Archive/Saved（预置一条收藏）。检查页面横向溢出、正文及元数据字号、可见控件44px目标、一个h1、标题层级、唯一ID；首页均为12条。
- **3 个附加布局场景**：375px空筛选、375px根字体放大200%、320px超长标题/来源+照片和字体请求失败+减少动态效果。此处放大的是根字体，**不是浏览器200%缩放认证**。
- **5 个规范样板宽度**：320/375/768/1024/1440px无页面级横向溢出，截图留档；样板原生dialog的ESC关闭与焦点归还通过。
- 行为检查：键盘跳过导航、可见焦点、搜索空状态、ESC关闭及焦点归还、游客收藏恢复、本机收藏/已读独立、移除最后一条收藏后的焦点、清空筛选、整期确认/刷新持久化、视频同意前不加载iframe。
- Chrome 的非空原生search输入第一次ESC清空查询，第二次ESC关闭弹窗，均按实际行为检查。
- 浏览器测试未记录未捕获JavaScript异常（`pageerror`）。未将此结果扩大为所有网络请求、控制台warning或外部播放器无错误。
- 逐组合自动截图；人工重点复核双语首页桌面/手机、详情、归档、收藏及搜索/视频弹窗。没有声称逐像素比对了全部截图。

### 回归中实际发现并修复

1. 收藏页标题从h1跳到h3：列表计数改为h2，保留原文案。
2. 英文手机“Today”点击宽度不足44px：导航源定义补齐min-width，样板同步。
3. 根字体放大200%时页眉操作和视频缩略图溢出：允许操作区换行，缩略图受可用宽度约束；没有隐藏内容或缩小正文规避。
4. 渲染后弹窗原触发元素可能被替换：按原操作恢复等价按钮；取消最后一项收藏时焦点归还主区域。

## 执行环境与证据

- Node来自Codex本机运行时；jsdom30.0.1位于 `/tmp/abespresso-review/node_modules`，Playwright来自随附运行时。网站本身仍无npm运行依赖。
- 命令与可配置参数见根级README；浏览器检查需先启动4173端口本地服务。
- 本轮完整临时截图/JSON在 `/tmp/abespresso-review/browser`；关键截图及结果副本在 `design/reviews/v1.2/`。

## Token 对比度（v1.2 重新计算）

| 配对 | 比值 |
| --- | ---: |
| ink/paper | 14.67:1 |
| muted/paper | 5.62:1 |
| muted/surface | 4.98:1 |
| accent/paper | 6.77:1 |
| on-accent/accent | 6.97:1 |
| on-accent/accent-hover | 9.09:1 |
| accent/accent-soft | 6.24:1 |
| success/success-soft | 6.45:1 |
| warning/warning-soft | 5.81:1 |
| danger/danger-soft | 5.78:1 |
| control-border/paper | 4.02:1 |
| focus/paper | 6.41:1 |
| ink/surface | 13.01:1 |
| accent/surface | 6.00:1 |
| accent-hover/paper | 8.83:1 |
| control-border/surface | 3.56:1 |
| focus/surface | 5.68:1 |
| brand/surface | 3.60:1 |

**明确禁止的普通文本配对：** 白字 / brand 只有 4.18:1；steam / paper 约 2.16:1。亮洋红用于图形与少量非文本强调，蒸汽暖灰仅装饰。上述测试不证明图片文字、透明叠加或全部历史 CSS 都合格。

## 迁移边界与未验证项

- `styles.css`（外壳、控件、浮层）与 `architecture.css`（页面编辑组件）已按源定义统一字号、间距与断点；这取代v1.1“仅颜色/字体家族迁移”的状态。未修改历史ZIP和品牌设计稿。
- Barlow400/600/700本地TTF保留且结构/许可检查通过；**没有打包Noto Sans SC**，中文仍依赖本机字体及无衬线回退。没有完成逐字形字体归属、CJK分片、WOFF2预算验收。
- 实际布局检查是Chrome；未验证Safari、Firefox、跨操作系统、屏幕阅读器、浏览器200%缩放和400%回流，不宣称WCAG全站认证。
- 详情行为覆盖全部12条×双语；五宽度布局矩阵只检查首条详情，不等于24份详情全部宽度均已人工复核。
- 故障场景确认首页无溢出、标题和核心操作保留，不等于已完成所有照片的失败占位/重试组件；真实内容加载、后台错误和网络重试尚无数据服务可验。
- 没有测量Lighthouse、LCP、CLS或加载速度，不宣称性能提速。视频仅核验同意前不连接播放器；实际YouTube播放未验证。
- 仍是明确标注的静态内容示例；未接入真实新闻、身份、云同步或发布后台。固定示例日期仍为2026-09-14，并非当天真实新闻。

## 历史版本说明

v1.0完成10项设计契约；v1.1扩为17项，保留55项业务检查并验证品牌颜色/资产。本轮保留原检查再新增，不以旧版测试代替当前浏览器回归。v1.1曾存在详情操作窄尺寸与旧字号/断点等迁移遗留，本次在真实组件中处理；剩余范围以上文为准。
